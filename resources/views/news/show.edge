@layout('layouts/main')

@section('content')
    <div class="container mt-5 text-center">
        <img src="{{ article.image }}" alt="img-fluid">
        <h1 class="display-5">{{ article.tittle}}</h1>

        <p class="lead">
            {{ article.content}}
        </p>
    </div>
@end